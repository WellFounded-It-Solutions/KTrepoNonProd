package se.infomaker.profile.view.items.authentication

/*
class AuthenticationItemProvider : PreviewParameterProvider<AuthPreviewParams> {
    override val values: Sequence<AuthPreviewParams> =
        sequenceOf(
            AuthPreviewParams(
                AuthenticationItemConfig(
                    title = "TITLE",
                ),
                AuthenticationState(isLoggedIn = true)
            ),
            AuthPreviewParams(
                AuthenticationItemConfig(
                    title = "TITLE",
                ),
                AuthenticationState(isLoggedIn = false)
            )
        )
}*/
