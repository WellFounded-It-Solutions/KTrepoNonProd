<manifest package="com.navigaglobal.mobile.consent.fundingchoices"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission
        android:name="android.permission.INTERNET"/>
    <application>

        <provider
            android:exported="false"
            android:authorities="${applicationId}.consent.InitFundingChoices"
            android:name=".InitFundingChoices" />
    </application>
</manifest>
